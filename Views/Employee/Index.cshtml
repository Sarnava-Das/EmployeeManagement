@model IEnumerable<EmployeeManagement.Models.Employee>  
    <!-- Employee list model -->
@{
    ViewData["Title"] = "Manage Employees";              <!-- Page title -->
}

<div class="card emp-card shadow-sm mt-4">               <!-- Main card container -->

    <!-- HEADER -->
    <div class="emp-header d-flex justify-content-between align-items-center"> 
        <h5 class="mb-0">Manage Employees</h5>             <!-- Header title -->
        <div>
            <!-- BULK DELETE -->
            <button type="button"
                    class="btn btn-danger btn-sm me-2"
                    onclick="openBulkDeleteModal()">       <!-- Bulk delete button -->
                <i class="bi bi-dash-circle"></i> Delete
            </button>

            <!-- ADD -->
            <button type="button"
                    class="btn btn-success btn-sm"
                    data-bs-toggle="modal"
                    data-bs-target="#addModal">            <!-- Open add employee modal -->
                <i class="bi bi-plus-circle"></i> Add New Employee
            </button>
        </div>
    </div>

    <!-- TABLE -->
    <div class="card-body p-0">                            <!-- Card body -->
        <form id="deleteForm" method="post" asp-action="DeleteMultiple"> <!-- Bulk delete form -->
            <table class="table emp-table mb-0 align-middle"> <!-- Employee table -->
                <thead>
                    <tr>
                        <th class="ps-3">
                            <input type="checkbox" id="selectAll" /> <!-- Select all checkbox -->
                        </th>
                        <th>Name</th>                       <!-- Name column -->
                        <th>Email</th>                      <!-- Email column -->
                        <th>Address</th>                    <!-- Address column -->
                        <th>Phone</th>                      <!-- Phone column -->
                        <th class="text-center">Actions</th> <!-- Action buttons column -->
                    </tr>
                </thead>

                <tbody>
                @foreach (var item in Model)     @* Loop through employees  *@
                {
                    <tr>
                        <td class="ps-3">
                            <input type="checkbox"
                                   class="row-checkbox"
                                   name="ids"
                                   value="@item.Id" />      <!-- Row checkbox -->
                        </td>
                        <td>@item.Name</td>                 
                        <!-- Employee name -->
                        <td>@item.Email</td>                <!-- Employee email -->
                        <td>@item.Address</td>              <!-- Employee address -->
                        <td>@item.Phone</td>                <!-- Employee phone -->
                        <td class="text-center">
                            <!-- EDIT -->
                            <button type="button"
                                    class="btn btn-warning btn-sm action-btn me-1"
                                    onclick="loadEdit(@item.Id)"> <!-- Open edit modal -->
                                <i class="bi bi-pencil"></i>
                            </button>

                            <!-- SINGLE DELETE -->
                            <button type="button"
                                    class="btn btn-danger btn-sm action-btn"
                                    onclick="openDeleteModal(@item.Id)"> <!-- Open delete modal -->
                                <i class="bi bi-trash"></i>
                            </button>
                        </td>
                    </tr>
                }
                </tbody>
            </table>
        </form>
    </div>

    <!-- PAGINATION -->
    @if (ViewBag.TotalPages > 1)        @* Show pagination if needed  *@
    {
        <div class="d-flex justify-content-between align-items-center px-3 py-2 border-top">
            <span class="text-muted">
                Showing page @ViewBag.CurrentPage of @ViewBag.TotalPages <!-- Page info -->
            </span>

            <nav>
                <ul class="pagination pagination-sm mb-0">  <!-- Pagination controls -->
                    <li class="page-item @(ViewBag.CurrentPage == 1 ? "disabled" : "")">
                        <a class="page-link"
                           asp-action="Index"
                           asp-route-page="@(ViewBag.CurrentPage - 1)"> <!-- Previous page -->
                            Previous
                        </a>
                    </li>

                    @for (int i = 1; i <= ViewBag.TotalPages; i++)  @* Page numbers  *@
                    {
                        <li class="page-item @(i == ViewBag.CurrentPage ? "active" : "")">
                            <a class="page-link"
                               asp-action="Index"
                               asp-route-page="@i">          <!-- Page link -->
                                @i
                            </a>
                        </li>
                    }

                    <li class="page-item @(ViewBag.CurrentPage == ViewBag.TotalPages ? "disabled" : "")">
                        <a class="page-link"
                           asp-action="Index"
                           asp-route-page="@(ViewBag.CurrentPage + 1)"> <!-- Next page -->
                            Next
                        </a>
                    </li>
                </ul>
            </nav>
        </div>
    }
</div>

<!-- PARTIAL VIEWS -->
@await Html.PartialAsync("_CreateEmployee", new EmployeeManagement.Models.Employee()) <!-- Add modal -->
@await Html.PartialAsync("_DeleteEmployee", new EmployeeManagement.Models.Employee()) <!-- Delete modal -->

<!-- EDIT MODAL CONTAINER (loaded dynamically) -->
<div class="modal fade" id="editModal" tabindex="-1">     <!-- Edit modal -->
    <div class="modal-dialog">
        <div class="modal-content" id="editContent"></div> <!-- Dynamic edit content -->
    </div>
</div>
